apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: forecastle
  namespace: openshift-gitops
spec:
  destination:
    namespace: stakater-forecastle
    server: https://kubernetes.default.svc
  project: default
  source:
    path: ocp-infra/cluster-dev/forecastle
    repoURL:  https://github.com/stakater/openshift-infra-gitops-example
    targetRevision: basic
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
